# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# graphify generated knowledge graph (regenerated via `graphify update .`)
graphify-out/

# OS / editor junk
.DS_Store
*:Zone.Identifier

# Stray scratchpad dirs (real scratchpad lives in /tmp)
scratchpad/
.superpowers/

# Local secrets — never commit (also asserted in Task 2)
backend/.env
.env

# Runtime evaluation store (durable-latest; not source)
FSM_Scoring_Agent/backend/data/store/

# Local dependency target used when system venv support is unavailable
FSM_Scoring_Agent/backend/.deps/

# Uploaded proposal files are runtime input, not source
FSM_Scoring_Agent/backend/data/uploads/

# Shared app credential and the user roster for the deployed evaluation agent.
# This repo is public. The analysis belongs here; the login sheet does not.
FSM_Scoring_Agent/docs/guides/LOGIN_INFO.md

# UI captures of live evaluations: real vendor names, real scores, a real
# recommendation, footer-marked Confidential & Proprietary. Public repo.
FSM_Scoring_Agent/docs/screenshots/
