# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# graphify generated knowledge graph (regenerated via `graphify update .`)
graphify-out/

# OS / editor junk
.DS_Store
*:Zone.Identifier

# Stray scratchpad dirs (real scratchpad lives in /tmp)
scratchpad/
.superpowers/

# Local secrets — never commit (also asserted in Task 2)
backend/.env
.env

# Runtime evaluation store (durable-latest; not source)
FSM_Scoring_Agent/backend/data/store/
